<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Record details</title>
    <link rel="stylesheet" href="/app.css" />
    <link rel="stylesheet" href="/header.css" />
    <link rel="stylesheet" href="/sidebar.css" />
    <link rel="stylesheet" href="/details.css" />
  </head>
  <body>
    <%- include('header') %>
    <div id="display">
      <%- include('sidebar', { artists: artists }) %>
      <div class="content">
        <div class="content-header">
          <h1>Details for <%= record.title %> by <%= record.artist %></h1>
        </div>
        <div id="detailed-record">
          <img class="record-art" src="<%= record.art %>" alt="" />
          <div id="details">
            <div id="button-wrapper">
              <a
                href="/artists/<%= locals.record.artist_id %>/<%= locals.record.id %>/update"
                ><button class="update">Edit</button></a
              >
              <form
                action="/artists/<%= locals.record.artist_id %>/<%= locals.record.id %>/delete"
                method="post"
              >
                <button
                  class="delete"
                  name="record_id"
                  value="<%= record.id %>"
                  type="submit"
                >
                  Delete
                </button>
              </form>
            </div>
            <div id="record-info">
              <ul id="row-one">
                <li><strong>Title:</strong> <%= record.title %></li>
                <li><strong>Artist:</strong> <%= record.artist %></li>
              </ul>
              <ul id="row-two">
                <li><strong>Release year:</strong> <%= record.yr %></li>
                <li><strong>Genre:</strong> <%= record.genre %></li>
                <li><strong>Released by:</strong> <%= record.label %></li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
